{
  "name": "ghat",
  "version": "1.0.0",
  "description": "ghat script",
  "scripts": {
    "build": "node ./scripts/build.js",
    "bundle": "tar czvf ./dist/bundle.tar.gz ./build"
  },
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "fs-extra": "^10.0.0"
  }
}
