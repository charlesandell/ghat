{
  "name": "ghat",
  "version": "1.0.2",
  "description": "ghat script",
  "scripts": {
    "build": "node ./scripts/build.js",
    "bundle": "npm run build && cross-env-shell \"tar czvf ./dist/$npm_package_name-v$npm_package_version.tar.gz -C build index.js lib\""
  },
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "fs-extra": "^10.0.0"
  }
}
